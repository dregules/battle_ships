require 'grid'

describe Grid do
  let(:board) { double('board', :placed_ships => [] , :board_size => 5, :ship_positions => 'A6')}

  # it 'receives the ship position' do
  #   ship = BattleShip.destroyer
  #   ship.place(board, 'A6', 'horizontal')
  #   expect(subject.receive_coords).to be ship_positions

  # end

  xit '#receives placed_ships'

  xit 'ships not overlap' do

  end

  xit 'positions the ship correctly' do

  end

  xit 'checks the ship state' do

  end


end
